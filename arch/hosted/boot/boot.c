
/**
 * Hosted architecture entry point.
 *
 * This "architecture" is not real, but rather a hosted user-mode environment
 * which mocks low-level OS operations with standard library calls.
 */
int main(void)
{
    return 0;
}
